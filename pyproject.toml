[tool.poetry]
name = "py playground"
version = "0.1.0"
description = "A simple Hello World package"
authors = ["Dominik Kirchner <dominik.kirchner2@de.bscoh.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
